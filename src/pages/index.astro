---
import Base from '../layouts/Base.astro';

const BASE = import.meta.env.BASE_URL.replace(/\/$/, '') + '/';
---

<Base title="RedRock">
  <section class="hero">
    <h1>RedRock 友链放送</h1>
    <p>把朋友们的 RSS 汇成一个个频道，轻松发现最近更新。</p>
    <a class="cta" href={`${BASE}tv/`}>浏览文章</a>
  </section>
  <style>
    .hero {
      max-width: 720px;
      margin: 8vh auto 0;
      text-align: center;
    }
    .hero h1 {
      font-size: 40px;
      letter-spacing: 1px;
      margin: 0 0 16px;
    }
    .hero p {
      color: var(--muted);
      margin: 0 0 28px;
    }
    .cta {
      display: inline-block;
      padding: 12px 28px;
      border-radius: 999px;
      background: var(--accent);
      color: #111;
      font-weight: 700;
      text-decoration: none;
    }
  </style>
</Base>
